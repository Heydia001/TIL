# PostgreSQL SELECT 기본

## 배운 것
- SELECT로 테이블 데이터 조회
- WHERE로 조건 필터링
- ORDER BY로 정렬

## 예시 쿼리
SELECT * FROM users WHERE age > 20 ORDER BY created_at DESC;

## 몰랐던 것
- fetchall()은 리스트 반환
- fetchone()은 없으면 None 반환